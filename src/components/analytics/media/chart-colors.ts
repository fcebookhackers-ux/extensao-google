export const mediaChartColors = {
  used: "hsl(var(--primary))",
  remaining: "hsl(var(--muted))",
  image: "hsl(var(--brand-primary-light))",
  video: "hsl(var(--secondary))",
  document: "hsl(var(--accent))",
  audio: "hsl(var(--ring))",
} as const;
